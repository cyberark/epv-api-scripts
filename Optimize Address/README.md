# Optimize Address


## Main capabilities
-----------------
- Updates the address fields on Windows based accounts to allow for Account Discovery to discover them

## Parameters:
```powershell
Optimize-Addresses.ps1 [-logonToken] [-Safes] [-ReportPath ".\AccountReport.csv"] [-DisableSSLVerify] 
```
